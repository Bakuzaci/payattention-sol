# PayAttention.sol

Solana meme coin tracker. Real data from CoinGecko.

## Backend

```bash
cd backend
pip install -r requirements.txt
python main.py
```

## Frontend

```bash
cd frontend
npm install
npm run dev
```

Set `VITE_API_URL` to your backend URL in production.
